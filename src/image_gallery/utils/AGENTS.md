# `utils/` 模块指南

## 职责

- 保留真正跨领域、无业务所有权的通用工具命名空间。
- 当前没有公开工具 API。

## 开发边界

- 优先把 helper 放在拥有该语义的领域模块中。
- 只有被多个领域复用且不依赖具体业务契约时才进入 `utils/`。
- 不将 `utils/` 作为无法分类代码的收纳目录。
- 当前行为：`openspec/specs/utils/spec.md`
