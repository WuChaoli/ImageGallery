# Examples

This directory contains runnable examples for the Python package API.

- `stage0_package_import.py`: verifies the package can be imported.
- `dataset_manager_demo/`: 使用 20 张本地真实图片和真实 PostgreSQL/pgvector、PyIceberg、MinIO Backend 的中文 DatasetManager 生命周期 Notebook。
