# 阶段 2 图片导入闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把本地目录、已有数据集文件和基础 URL 清单稳定转换为平台内 raw Dataset、import_report 和 failure_manifest。

**Architecture:** 阶段 2 只编排导入流程：Source Reader 读取外部来源，Storage Writer 写入阶段 1 的 Storage，Metadata Extractor 抽取基础事实字段，Raw Dataset Builder 写出 Parquet。失败样本写入 failure_manifest，单张失败不阻断整批导入。

**Tech Stack:** Python package, PyArrow/Pandas, Pillow, requests, pathlib, hashlib, uuid, pytest.

---

## 1. 计划依据

1. `docs/development/图片数据集处理框架-顶层开发计划.md`
2. `docs/development/阶段1-公共基础能力开发计划.md`
3. `docs/development/阶段1-Storage开发计划.md`
4. `docs/architecture/modules/图片导入与元数据.md`
5. `docs/architecture/modules/存储系统.md`
6. `docs/architecture/modules/数据集管理与可视化.md`
7. `docs/architecture/modules/错误处理与恢复.md`

## 2. 前置条件

阶段 2 依赖阶段 1。

- [ ] **Step 1: 验证阶段 1 公共能力**

Run:

```bash
.venv/bin/python -m pytest tests/unit/storage tests/unit/dataset tests/unit/schemas -q
.venv/bin/python examples/stage1_storage_dataset_quickstart.py
```

Expected: pytest PASS；quickstart 第一行输出 `1`，第二行输出 64 字符 fingerprint。若失败，先修复阶段 1，不在阶段 2 中改写 Storage、Dataset 或 Raw Schema 的边界。

## 3. 文件结构

阶段 2 预计创建或修改以下文件：

```text
src/image_gallery/importers/
  __init__.py
  config.py
  errors.py
  local_directory.py
  dataset_file.py
  url_list.py
  metadata.py
  pipeline.py
  report.py
tests/unit/importers/
  test_local_directory_reader.py
  test_dataset_file_reader.py
  test_metadata.py
  test_import_pipeline.py
  test_url_list_reader.py
examples/
  stage2_import_local_directory.py
```

责任边界：

1. `SourceRecord` 只描述外部来源条目，不包含清洗判断。
2. `ImportPipeline` 只输出 raw Dataset、import_report 和 failure_manifest。
3. `metadata.py` 只抽取基础事实：文件大小、checksum、格式、宽高、通道和颜色模式。
4. URL 导入只支持 http/https、大小限制、超时、localhost/内网地址拒绝和基础 content-type 校验。

## 4. Task 1: SourceRecord 和本地目录读取

**Files:**
- Create or modify: `src/image_gallery/importers/config.py`
- Create or modify: `src/image_gallery/importers/local_directory.py`
- Create or modify: `src/image_gallery/importers/__init__.py`
- Test: `tests/unit/importers/test_local_directory_reader.py`

- [ ] **Step 1: Write failing tests for local directory reader**

Create `tests/unit/importers/test_local_directory_reader.py`:

```python
from pathlib import Path

from image_gallery.importers import LocalDirectoryReader


def test_local_directory_reader_scans_supported_images(tmp_path: Path) -> None:
    (tmp_path / "a.jpg").write_bytes(b"jpg")
    (tmp_path / "b.txt").write_text("not image", encoding="utf-8")
    nested = tmp_path / "nested"
    nested.mkdir()
    (nested / "c.png").write_bytes(b"png")

    records = list(LocalDirectoryReader(tmp_path).read())

    assert [record.source_file_name for record in records] == ["a.jpg", "c.png"]
    assert records[0].source_type == "local_directory"
    assert records[0].source_uri == str(tmp_path / "a.jpg")
    assert records[1].source_relative_path == "nested/c.png"


def test_local_directory_reader_is_deterministic(tmp_path: Path) -> None:
    (tmp_path / "z.jpg").write_bytes(b"z")
    (tmp_path / "a.jpg").write_bytes(b"a")

    records = list(LocalDirectoryReader(tmp_path).read())

    assert [record.source_file_name for record in records] == ["a.jpg", "z.jpg"]
```

- [ ] **Step 2: Run local directory reader tests to verify they fail**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_local_directory_reader.py -q
```

Expected: FAIL because `image_gallery.importers` does not exist or `LocalDirectoryReader` is missing.

- [ ] **Step 3: Implement SourceRecord and LocalDirectoryReader**

Create `src/image_gallery/importers/config.py`:

```python
from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class SourceRecord:
    """外部图片来源条目，导入前不承载清洗语义。"""

    source_uri: str
    source_type: str
    source_file_name: str
    source_relative_path: str
    local_path: Path | None = None
```

Create `src/image_gallery/importers/local_directory.py`:

```python
from pathlib import Path

from image_gallery.importers.config import SourceRecord


SUPPORTED_IMAGE_SUFFIXES = {".jpg", ".jpeg", ".png", ".bmp", ".gif", ".webp"}


class LocalDirectoryReader:
    """递归扫描本地目录中的图片文件。"""

    def __init__(self, root: str | Path) -> None:
        self.root = Path(root).expanduser().resolve()

    def read(self) -> list[SourceRecord]:
        records: list[SourceRecord] = []
        for path in sorted(self.root.rglob("*")):
            if not path.is_file() or path.suffix.lower() not in SUPPORTED_IMAGE_SUFFIXES:
                continue
            records.append(
                SourceRecord(
                    source_uri=str(path),
                    source_type="local_directory",
                    source_file_name=path.name,
                    source_relative_path=path.relative_to(self.root).as_posix(),
                    local_path=path,
                )
            )
        return records
```

Create `src/image_gallery/importers/__init__.py`:

```python
from image_gallery.importers.config import SourceRecord
from image_gallery.importers.local_directory import LocalDirectoryReader

__all__ = ["LocalDirectoryReader", "SourceRecord"]
```

- [ ] **Step 4: Run local directory reader tests to verify they pass**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_local_directory_reader.py -q
```

Expected: PASS.

- [ ] **Step 5: Commit local directory reader**

Run:

```bash
git add src/image_gallery/importers tests/unit/importers/test_local_directory_reader.py
git commit -m "feat: add local directory source reader"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 5. Task 2: 已有数据集文件读取

**Files:**
- Create or modify: `src/image_gallery/importers/dataset_file.py`
- Modify: `src/image_gallery/importers/__init__.py`
- Test: `tests/unit/importers/test_dataset_file_reader.py`

- [ ] **Step 1: Write failing tests for dataset file reader**

Create `tests/unit/importers/test_dataset_file_reader.py`:

```python
from pathlib import Path

import pandas as pd

from image_gallery.importers import DatasetFileReader


def test_dataset_file_reader_reads_image_uri_column(tmp_path: Path) -> None:
    dataset_uri = tmp_path / "source.parquet"
    pd.DataFrame(
        [
            {"image_uri": str(tmp_path / "a.jpg"), "source_uri": "original://a"},
            {"image_uri": str(tmp_path / "b.jpg"), "source_uri": "original://b"},
        ]
    ).to_parquet(dataset_uri, index=False)

    records = list(DatasetFileReader(str(dataset_uri)).read())

    assert [record.source_uri for record in records] == ["original://a", "original://b"]
    assert records[0].source_type == "dataset_file"
    assert records[0].local_path == Path(tmp_path / "a.jpg")


def test_dataset_file_reader_falls_back_to_image_uri_as_source_uri(tmp_path: Path) -> None:
    dataset_uri = tmp_path / "source.csv"
    pd.DataFrame([{"image_uri": str(tmp_path / "a.jpg")}]).to_csv(dataset_uri, index=False)

    records = list(DatasetFileReader(str(dataset_uri)).read())

    assert records[0].source_uri == str(tmp_path / "a.jpg")
```

- [ ] **Step 2: Run dataset file reader tests to verify they fail**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_dataset_file_reader.py -q
```

Expected: FAIL because `DatasetFileReader` is missing.

- [ ] **Step 3: Implement DatasetFileReader**

Create `src/image_gallery/importers/dataset_file.py`:

```python
from pathlib import Path

from image_gallery.dataset import Dataset
from image_gallery.importers.config import SourceRecord


class DatasetFileReader:
    """从已有 Dataset 文件读取待导入图片清单。"""

    def __init__(self, dataset_uri: str) -> None:
        self.dataset_uri = dataset_uri

    def read(self) -> list[SourceRecord]:
        frame = Dataset.from_uri(self.dataset_uri).to_frame()
        records: list[SourceRecord] = []
        for row in frame.to_dict("records"):
            image_uri = str(row["image_uri"])
            source_uri = str(row.get("source_uri") or image_uri)
            path = Path(image_uri)
            records.append(
                SourceRecord(
                    source_uri=source_uri,
                    source_type="dataset_file",
                    source_file_name=path.name,
                    source_relative_path=path.name,
                    local_path=path if path.exists() or path.is_absolute() else None,
                )
            )
        return records
```

Modify `src/image_gallery/importers/__init__.py`:

```python
from image_gallery.importers.config import SourceRecord
from image_gallery.importers.dataset_file import DatasetFileReader
from image_gallery.importers.local_directory import LocalDirectoryReader

__all__ = ["DatasetFileReader", "LocalDirectoryReader", "SourceRecord"]
```

- [ ] **Step 4: Run dataset file reader tests to verify they pass**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_dataset_file_reader.py -q
```

Expected: PASS.

- [ ] **Step 5: Commit dataset file reader**

Run:

```bash
git add src/image_gallery/importers/dataset_file.py src/image_gallery/importers/__init__.py tests/unit/importers/test_dataset_file_reader.py
git commit -m "feat: add dataset file source reader"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 6. Task 3: 基础元数据抽取

**Files:**
- Create or modify: `src/image_gallery/importers/metadata.py`
- Test: `tests/unit/importers/test_metadata.py`

- [ ] **Step 1: Write failing metadata tests**

Create `tests/unit/importers/test_metadata.py`:

```python
from pathlib import Path

from PIL import Image

from image_gallery.importers.metadata import extract_basic_metadata


def test_extract_basic_metadata_reads_image_facts(tmp_path: Path) -> None:
    image_path = tmp_path / "a.jpg"
    Image.new("RGB", (16, 8), color="red").save(image_path)

    metadata = extract_basic_metadata(image_path)

    assert metadata["file_size_bytes"] > 0
    assert metadata["checksum"].startswith("sha256:")
    assert metadata["image_format"] == "JPEG"
    assert metadata["width"] == 16
    assert metadata["height"] == 8
    assert metadata["channels"] == 3
    assert metadata["color_mode"] == "RGB"
```

- [ ] **Step 2: Run metadata tests to verify they fail**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_metadata.py -q
```

Expected: FAIL because `extract_basic_metadata` is missing.

- [ ] **Step 3: Implement metadata extractor**

Create `src/image_gallery/importers/metadata.py`:

```python
import hashlib
from pathlib import Path
from typing import Any

from PIL import Image


def extract_basic_metadata(path: str | Path) -> dict[str, Any]:
    """抽取导入阶段 raw Dataset 需要的低成本基础事实。"""
    image_path = Path(path)
    data = image_path.read_bytes()
    checksum = "sha256:" + hashlib.sha256(data).hexdigest()
    with Image.open(image_path) as image:
        bands = image.getbands()
        return {
            "file_size_bytes": len(data),
            "checksum": checksum,
            "image_format": image.format or image_path.suffix.lstrip(".").upper(),
            "width": image.width,
            "height": image.height,
            "channels": len(bands),
            "color_mode": image.mode,
        }
```

- [ ] **Step 4: Run metadata tests to verify they pass**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_metadata.py -q
```

Expected: PASS.

- [ ] **Step 5: Commit metadata extractor**

Run:

```bash
git add src/image_gallery/importers/metadata.py tests/unit/importers/test_metadata.py
git commit -m "feat: add basic import metadata extractor"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 7. Task 4: ImportPipeline 输出 raw Dataset、report 和 failure_manifest

**Files:**
- Create or modify: `src/image_gallery/importers/errors.py`
- Create or modify: `src/image_gallery/importers/report.py`
- Create or modify: `src/image_gallery/importers/pipeline.py`
- Modify: `src/image_gallery/importers/__init__.py`
- Test: `tests/unit/importers/test_import_pipeline.py`

- [ ] **Step 1: Write failing pipeline tests**

Create `tests/unit/importers/test_import_pipeline.py`:

```python
from pathlib import Path

import pandas as pd
from PIL import Image

from image_gallery.dataset import Dataset
from image_gallery.importers import ImportPipeline, LocalDirectoryReader
from image_gallery.storage import StorageRegistry


def test_import_pipeline_writes_raw_dataset_report_and_failures(tmp_path: Path) -> None:
    source_dir = tmp_path / "source"
    source_dir.mkdir()
    Image.new("RGB", (4, 2), color="blue").save(source_dir / "a.jpg")
    (source_dir / "bad.jpg").write_bytes(b"not an image")

    registry = StorageRegistry.from_config(
        {
            "default_storage": "local_main",
            "storages": [
                {"name": "local_main", "type": "filesystem", "root": str(tmp_path / "storage")},
            ],
        }
    )
    pipeline = ImportPipeline(storage_registry=registry, output_dir=tmp_path / "outputs")

    result = pipeline.run(LocalDirectoryReader(source_dir).read())

    raw_frame = Dataset.from_uri(result.raw_dataset_uri).to_frame()
    failure_frame = pd.read_json(result.failure_manifest_uri, lines=True)

    assert raw_frame["image_uri"].str.contains(str(tmp_path / "storage")).all()
    assert raw_frame.iloc[0]["import_status"] == "imported"
    assert result.report["success_count"] == 1
    assert result.report["failure_count"] == 1
    assert failure_frame.iloc[0]["source_uri"].endswith("bad.jpg")
    assert failure_frame.iloc[0]["error_stage"] == "metadata"
```

- [ ] **Step 2: Run pipeline tests to verify they fail**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_import_pipeline.py -q
```

Expected: FAIL because `ImportPipeline` is missing.

- [ ] **Step 3: Implement import result and pipeline**

Create `src/image_gallery/importers/report.py`:

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class ImportResult:
    """导入闭环的三个标准产物入口。"""

    raw_dataset_uri: str
    import_report_uri: str
    failure_manifest_uri: str
    report: dict[str, int]
```

Create `src/image_gallery/importers/pipeline.py`:

```python
import json
import uuid
from datetime import UTC, datetime
from pathlib import Path
from typing import Iterable

import pandas as pd

from image_gallery.dataset import Dataset
from image_gallery.importers.config import SourceRecord
from image_gallery.importers.metadata import extract_basic_metadata
from image_gallery.importers.report import ImportResult
from image_gallery.schemas import validate_raw_dataset
from image_gallery.storage import StorageRegistry


class ImportPipeline:
    """把 SourceRecord 转换为受管 storage 中的 raw Dataset。"""

    def __init__(self, storage_registry: StorageRegistry, output_dir: str | Path, storage_name: str | None = None) -> None:
        self.storage_registry = storage_registry
        self.output_dir = Path(output_dir)
        self.storage_name = storage_name

    def run(self, records: Iterable[SourceRecord]) -> ImportResult:
        self.output_dir.mkdir(parents=True, exist_ok=True)
        storage = self.storage_registry.connect(self.storage_name)
        imported_rows: list[dict[str, object]] = []
        failures: list[dict[str, object]] = []

        for record in records:
            imported_at = datetime.now(UTC).isoformat()
            try:
                if record.local_path is None:
                    raise ValueError("local_path is required for current import mode")
                metadata = extract_basic_metadata(record.local_path)
                image_id = str(uuid.uuid4())
                object_path = f"images/raw/{image_id}/{record.source_file_name}"
                image_uri = storage.write_bytes(object_path, record.local_path.read_bytes(), overwrite=False)
                imported_rows.append(
                    {
                        "image_id": image_id,
                        "source_uri": record.source_uri,
                        "source_type": record.source_type,
                        "source_file_name": record.source_file_name,
                        "storage_name": storage.storage_name,
                        "image_uri": image_uri,
                        "import_status": "imported",
                        "imported_at": imported_at,
                        **metadata,
                    }
                )
            except Exception as exc:
                failures.append(
                    {
                        "source_uri": record.source_uri,
                        "source_type": record.source_type,
                        "error_stage": "metadata",
                        "error_code": exc.__class__.__name__,
                        "error_message": str(exc),
                        "retryable": True,
                        "occurred_at": imported_at,
                    }
                )

        raw_dataset_uri = str(self.output_dir / "raw.parquet")
        failure_manifest_uri = str(self.output_dir / "failure_manifest.jsonl")
        import_report_uri = str(self.output_dir / "import_report.json")

        raw_frame = pd.DataFrame(imported_rows)
        if not raw_frame.empty:
            validate_raw_dataset(raw_frame)
        Dataset.write(raw_frame, raw_dataset_uri)

        pd.DataFrame(failures).to_json(failure_manifest_uri, orient="records", lines=True, force_ascii=False)
        report = {"success_count": len(imported_rows), "failure_count": len(failures)}
        Path(import_report_uri).write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8")

        return ImportResult(
            raw_dataset_uri=raw_dataset_uri,
            import_report_uri=import_report_uri,
            failure_manifest_uri=failure_manifest_uri,
            report=report,
        )
```

Modify `src/image_gallery/importers/__init__.py`:

```python
from image_gallery.importers.config import SourceRecord
from image_gallery.importers.dataset_file import DatasetFileReader
from image_gallery.importers.local_directory import LocalDirectoryReader
from image_gallery.importers.pipeline import ImportPipeline
from image_gallery.importers.report import ImportResult

__all__ = [
    "DatasetFileReader",
    "ImportPipeline",
    "ImportResult",
    "LocalDirectoryReader",
    "SourceRecord",
]
```

- [ ] **Step 4: Run pipeline tests to verify they pass**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_import_pipeline.py -q
```

Expected: PASS.

- [ ] **Step 5: Commit import pipeline**

Run:

```bash
git add src/image_gallery/importers tests/unit/importers/test_import_pipeline.py
git commit -m "feat: add raw dataset import pipeline"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 8. Task 5: 基础 URL 清单导入边界

**Files:**
- Create or modify: `src/image_gallery/importers/url_list.py`
- Modify: `src/image_gallery/importers/__init__.py`
- Test: `tests/unit/importers/test_url_list_reader.py`

- [ ] **Step 1: Write failing tests for URL list validation**

Create `tests/unit/importers/test_url_list_reader.py`:

```python
from pathlib import Path

import pytest

from image_gallery.importers import UrlListReader


def test_url_list_reader_rejects_localhost(tmp_path: Path) -> None:
    url_list = tmp_path / "urls.txt"
    url_list.write_text("http://localhost/a.jpg\n", encoding="utf-8")

    with pytest.raises(ValueError, match="unsafe url host"):
        list(UrlListReader(url_list, download_dir=tmp_path / "downloads").read())


def test_url_list_reader_accepts_https_urls_as_source_records(tmp_path: Path) -> None:
    url_list = tmp_path / "urls.txt"
    url_list.write_text("https://example.com/a.jpg\n", encoding="utf-8")

    records = list(UrlListReader(url_list, download_dir=tmp_path / "downloads", download=False).read())

    assert records[0].source_uri == "https://example.com/a.jpg"
    assert records[0].source_type == "url_list"
    assert records[0].source_file_name == "a.jpg"


def test_url_list_reader_downloads_image_to_local_path(tmp_path: Path, monkeypatch) -> None:
    class FakeResponse:
        headers = {"content-type": "image/jpeg", "content-length": "3"}
        content = b"abc"

        def raise_for_status(self) -> None:
            return None

    def fake_get(url: str, timeout: int, allow_redirects: bool):
        assert url == "https://example.com/a.jpg"
        assert timeout == 10
        assert allow_redirects is True
        return FakeResponse()

    monkeypatch.setattr("requests.get", fake_get)
    url_list = tmp_path / "urls.txt"
    url_list.write_text("https://example.com/a.jpg\n", encoding="utf-8")

    records = list(UrlListReader(url_list, download_dir=tmp_path / "downloads").read())

    assert records[0].local_path == tmp_path / "downloads" / "a.jpg"
    assert records[0].local_path.read_bytes() == b"abc"
```

- [ ] **Step 2: Run URL list tests to verify they fail**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_url_list_reader.py -q
```

Expected: FAIL because `UrlListReader` is missing.

- [ ] **Step 3: Implement URL list reader validation boundary**

Create `src/image_gallery/importers/url_list.py`:

```python
import ipaddress
from pathlib import Path
from urllib.parse import urlparse

import requests

from image_gallery.importers.config import SourceRecord


class UrlListReader:
    """基础 URL 清单 reader，执行安全校验并可下载到本地临时文件。"""

    def __init__(
        self,
        url_list_path: str | Path,
        download_dir: str | Path,
        download: bool = True,
        timeout_seconds: int = 10,
        max_file_bytes: int = 50 * 1024 * 1024,
    ) -> None:
        self.url_list_path = Path(url_list_path)
        self.download_dir = Path(download_dir)
        self.download = download
        self.timeout_seconds = timeout_seconds
        self.max_file_bytes = max_file_bytes

    def read(self) -> list[SourceRecord]:
        records: list[SourceRecord] = []
        for line in self.url_list_path.read_text(encoding="utf-8").splitlines():
            url = line.strip()
            if not url:
                continue
            self._validate_url(url)
            file_name = Path(urlparse(url).path).name or "downloaded-image"
            local_path = self._download(url, file_name) if self.download else None
            records.append(
                SourceRecord(
                    source_uri=url,
                    source_type="url_list",
                    source_file_name=file_name,
                    source_relative_path=file_name,
                    local_path=local_path,
                )
            )
        return records

    def _validate_url(self, url: str) -> None:
        parsed = urlparse(url)
        if parsed.scheme not in {"http", "https"}:
            raise ValueError(f"unsupported url scheme: {parsed.scheme}")
        host = parsed.hostname or ""
        if host in {"localhost", "127.0.0.1", "::1"}:
            raise ValueError(f"unsafe url host: {host}")
        try:
            ip = ipaddress.ip_address(host)
        except ValueError:
            return
        if ip.is_private or ip.is_loopback or ip.is_link_local:
            raise ValueError(f"unsafe url host: {host}")

    def _download(self, url: str, file_name: str) -> Path:
        response = requests.get(url, timeout=self.timeout_seconds, allow_redirects=True)
        response.raise_for_status()
        content_type = response.headers.get("content-type", "").split(";")[0].lower()
        if not content_type.startswith("image/"):
            raise ValueError(f"unsupported content-type: {content_type}")
        content_length = int(response.headers.get("content-length") or len(response.content))
        if content_length > self.max_file_bytes:
            raise ValueError(f"url content too large: {content_length}")
        if len(response.content) > self.max_file_bytes:
            raise ValueError(f"url content too large: {len(response.content)}")
        self.download_dir.mkdir(parents=True, exist_ok=True)
        output_path = self.download_dir / file_name
        output_path.write_bytes(response.content)
        return output_path
```

Modify `src/image_gallery/importers/__init__.py`:

```python
from image_gallery.importers.config import SourceRecord
from image_gallery.importers.dataset_file import DatasetFileReader
from image_gallery.importers.local_directory import LocalDirectoryReader
from image_gallery.importers.pipeline import ImportPipeline
from image_gallery.importers.report import ImportResult
from image_gallery.importers.url_list import UrlListReader

__all__ = [
    "DatasetFileReader",
    "ImportPipeline",
    "ImportResult",
    "LocalDirectoryReader",
    "SourceRecord",
    "UrlListReader",
]
```

- [ ] **Step 4: Run URL list tests to verify they pass**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers/test_url_list_reader.py -q
```

Expected: PASS.

- [ ] **Step 5: Commit URL list boundary**

Run:

```bash
git add src/image_gallery/importers/url_list.py src/image_gallery/importers/__init__.py tests/unit/importers/test_url_list_reader.py
git commit -m "feat: add url list import boundary"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 9. Task 6: Stage 2 example and full import validation

**Files:**
- Create or modify: `examples/stage2_import_local_directory.py`
- Test: manual command in this task

- [ ] **Step 1: Create local directory import example**

Create `examples/stage2_import_local_directory.py`:

```python
from pathlib import Path

from PIL import Image

from image_gallery.dataset import Dataset
from image_gallery.importers import ImportPipeline, LocalDirectoryReader
from image_gallery.storage import StorageRegistry


def main() -> None:
    work_dir = Path("examples/.stage2_work").resolve()
    source_dir = work_dir / "source"
    source_dir.mkdir(parents=True, exist_ok=True)
    Image.new("RGB", (8, 8), color="green").save(source_dir / "green.jpg")

    registry = StorageRegistry.from_config(
        {
            "default_storage": "local_main",
            "storages": [
                {"name": "local_main", "type": "filesystem", "root": str(work_dir / "storage")},
            ],
        }
    )

    result = ImportPipeline(registry, output_dir=work_dir / "outputs").run(LocalDirectoryReader(source_dir).read())
    raw_dataset = Dataset.from_uri(result.raw_dataset_uri)
    print(raw_dataset.count())
    print(result.report["success_count"])
    print(result.report["failure_count"])


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Run full Stage 2 tests**

Run:

```bash
.venv/bin/python -m pytest tests/unit/importers -q
```

Expected: PASS.

- [ ] **Step 3: Run Stage 2 example**

Run:

```bash
.venv/bin/python examples/stage2_import_local_directory.py
```

Expected: stdout prints `1`, `1`, `0` on separate lines.

- [ ] **Step 4: Commit Stage 2 example**

Run:

```bash
git add examples/stage2_import_local_directory.py
git commit -m "docs: add stage2 local import example"
```

Expected: Commit succeeds in the project git repository. If the current checkout is not a git repository, record `git commit skipped: not a git repository` in the execution log and continue.

## 10. 阶段 2 验收标准

1. 可以从本地目录递归扫描图片并生成 `SourceRecord`。
2. 可以从已有 Parquet/CSV/JSONL 数据集文件读取图片清单。
3. URL 清单 reader 具备 http/https 白名单和 localhost、内网、链路本地地址拒绝边界。
4. 可以把本地图片写入阶段 1 Storage，并把返回的 `image_uri` 写入 raw Dataset。
5. raw Dataset 使用 `image_uri` 作为后续处理主引用，并通过 `validate_raw_dataset()` 校验。
6. 单张图片失败不阻断整批导入，失败进入 `failure_manifest.jsonl`。
7. `import_report.json` 至少包含成功数和失败数。
8. 阶段 2 不包含 CleaningRecipe、ExecutionPlan、node_result、node_status_matrix、MergePolicy、clean/dropped/full 归并或 SQLite 恢复逻辑。

## 11. Self-Review

Spec coverage:

1. 本地目录导入：Task 1 和 Task 4 覆盖。
2. 已有数据集文件导入：Task 2 覆盖。
3. 基础 URL 清单导入边界：Task 5 覆盖，包括安全校验、content-type 校验、大小限制和下载到本地临时文件。
4. 图片复制到统一 Storage：Task 4 覆盖。
5. 基础元数据抽取：Task 3 覆盖。
6. raw Dataset、import_report、failure_manifest：Task 4 和 Task 6 覆盖。

Placeholder scan:

1. 本计划没有遗留占位标记、空实现说明或跨任务省略写法。
2. URL 网络下载已纳入当前阶段最小闭环，单元测试通过 monkeypatch 固定网络响应，避免依赖真实外网。

Type consistency:

1. `SourceRecord` 字段在 LocalDirectoryReader、DatasetFileReader、UrlListReader 和 ImportPipeline 中一致。
2. `ImportPipeline.run()` 返回 `ImportResult`，测试和 example 使用相同字段名。
3. raw Dataset 字段与阶段 1 的 `RawDatasetSchema` 对齐。
