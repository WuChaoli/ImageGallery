# ImageGallery

ImageGallery is a local-first Python package for image dataset import, storage, cleaning, viewing, and export.

The first version focuses on Python package APIs and Jupyter validation. It does not include a server API or web UI.
